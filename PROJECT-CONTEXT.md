# PROJECT-CONTEXT.md

## 文档用途

本文件记录项目当前的背景、已确定的设计决策、开发状态和后续方向，供新对话恢复上下文使用。

长期不变的操作规则放在根目录 `AGENTS.md`；具体 Skill 的专业流程放在对应目录的 `SKILL.md`。

## 项目目标

本项目要建设一套面向股权投资流程的 Agent Skills，让 Agent 能够理解和处理 PDF、Word、Markdown、表格、会议纪要、财务资料、法律资料和外部公开信息，并辅助投资人完成从项目获取到投决的完整流程。

核心要求：

- Agent 负责理解、归纳、比较、追问、维护状态和起草分析材料；
- 不是固定关键词抽取程序；
- 所有重要结论都要有证据、来源和状态；
- 必须区分事实、管理层主张、外部资料、推导结果和模拟输入；
- 默认所有面向用户的输出使用简体中文；
- AI 不能替代律师、会计师、行业专家、客户访谈、现场核验和投资人最终责任。

## 已确定的总体流程

```text
项目材料
  → 项目卡、第一轮问题清单、访谈提纲
  → 会议纪要、新增数据、回答、承诺、矛盾
  → 更新后的项目卡和问题状态
  → 立项研究和资料需求清单
  → 尽调启动和统一问题台账
  → 商业尽调、财务尽调、法律尽调
  → 财务预测与估值
  → 投资条款分析
  → 投决会报告
```

开发和测试时遵循单阶段推进：一次只执行一个阶段，完成后汇报结果和缺口，再进入下一阶段。不能因为材料已经存在，就一次性生成全套投资流程文件。

## 已开发 Skill

当前仓库包含以下 11 个 Skill：

1. `project-intake-and-project-card`：项目初审、项目卡、第一轮问题和访谈提纲；
2. `update-project-card-from-meeting`：会议纪要和新增资料回写；
3. `project-initiation-research-and-memo`：立项研究和研究报告；
4. `due-diligence-intake-and-request-list`：尽调启动与资料清单；
5. `due-diligence-workspace-and-issue-ledger`：统一尽调工作区和问题台账；
6. `commercial-due-diligence-analysis`：商业尽调；
7. `financial-due-diligence-analysis`：财务尽调；
8. `legal-due-diligence-analysis`：法律与股权尽调；
9. `financial-forecast-and-valuation`：财务预测、估值和投资回报；
10. `investment-terms-analysis`：投资条款分析；
11. `investment-committee-decision-memo`：投决会报告。

每个 Skill 应保持独立、可调用，并通过稳定的问题 ID、来源、证据状态、投资影响和下游输入连接起来。

## 已确定的输出规范

每份输出至少应说明：

- 分析范围、主体、期间和数据截止日；
- 使用的材料；
- 已确认事实、管理层主张、外部支持、Agent 推导和模拟输入；
- 关键矛盾、资料缺口和不确定性；
- 对估值、投资金额、条款和投决的影响；
- 下一步核验动作和责任方；
- AI 分析边界以及需要专业人士复核的事项。

问题台账应使用稳定的 `issue_id`，例如 `COM-001`、`FIN-001`、`LEG-001`，并维护优先级、证据状态、投资影响、责任人、下一步和变更历史。

## 当前开发状态

- Skill 主体和配套 references 已经建立；
- README 已完成，包含项目目标、工作流、Skill 清单、中文输出规范、证据规则和隐私边界；
- `.gitignore` 已忽略真实项目输出、根目录 PDF、对话导出文件和本地案例资料；
- 真实项目材料和测试输出只保存在本地，不进入公开仓库；
- 当前重点从“继续堆叠案例测试”转向完善项目级上下文和长期协作规则。

## 案例与敏感资料约定

曾经使用过若干真实项目材料进行本地测试。这些材料、生成的项目卡、尽调报告、模型、会议输入和工作区均可能包含敏感信息，不能因为它们已被 `.gitignore` 忽略就上传到 GitHub。

Airbnb 公开案例资料也只作为本地测试资料保存。公开不等于适合直接放进通用 Skill 仓库；资料包应保留在 `.case-materials/`，不参与公开提交。

公开仓库只提交：

- 通用 Skill；
- Skill 的结构化输出约束和模板；
- 明确标注的合成案例；
- 项目规范、README 和开发文档。

## 当前未完成方向

- 继续检查所有 Skill 是否都严格遵守全中文输出要求；
- 统一各 Skill 的证据状态、问题 ID 和投资影响字段；
- 加强跨 Skill 的统一工作区和问题回写机制；
- 为每个 Skill 补充一致的输入、输出和质量验收说明；
- 逐步增加不含真实保密信息的合成测试案例；
- 在开发新 Skill 前，先确认它在完整投资流程中的位置，避免功能重叠。

## 新对话启动建议

新对话开始时，先告诉 Agent 当前目标，例如：

```text
请先读取 AGENTS.md、PROJECT-CONTEXT.md 和 README.md。
当前要开发/检查的是：<Skill 名称或具体目标>。
请只处理当前阶段，不要提前生成后续投资流程文件。
```

如果用户提供了新的真实项目材料，应先确认材料范围、保密属性、数据时点和允许的存储/同步范围，再开始分析。
